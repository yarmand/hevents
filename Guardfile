# A sample Guardfile
# More info at https://github.com/guard/guard#readme
#

guard :coffeescript, :input => 'src', :output => 'lib' do
end

guard :coffeescript, :input => 'tests', :output => 'tests' do
end


